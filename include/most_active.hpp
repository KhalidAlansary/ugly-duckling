#ifndef MOST_ACTIVE_HPP
#define MOST_ACTIVE_HPP

#include "util.hpp"

/**
 * @brief Find the most active user.
 *
 * @param users The list of users.
 * @return User The most active user.
 */
User most_active(const std::vector<User>& users);
#endif
