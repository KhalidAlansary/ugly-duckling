#ifndef Search_hpp
#define Search_hpp

#include <string>
#include <vector>

#include "parser.hpp"

std::vector<std::string> mutual(const ElementNode& root,
                                const std::string& keyword);

#endif
